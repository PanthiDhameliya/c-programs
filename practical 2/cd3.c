#include<stdio.h>
void main()
{
        int length,breadth,area,perimeter;
        length=70;
        breadth=90;
        area=length*breadth;
        perimeter=2*(length+breadth);
        printf("area of a the park= %d\n",area);
        printf("perimeter= %d",perimeter);
        return 0;

}
