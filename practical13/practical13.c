#include<stdio.h>
int main()
{
    int waterlevel;
    for(waterlevel=0;waterlevel<=100;waterlevel+=10)
    {
            printf("current water level : %d liters\n",waterlevel);
    }
            printf("Tank is full");
}
